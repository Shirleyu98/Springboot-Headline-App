apiVersion: v1
kind: Service
metadata:
  name: {{ include "headline-app.fullname" . }}
  labels:
    {{- include "headline-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "headline-app.selectorLabels" . | nindent 4 }}
